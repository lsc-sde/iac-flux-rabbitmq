apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: rabbitmq-cluster
  namespace: rabbitmq
namespace: rabbitmq
resources:
  - cluster.yaml

spec:
  dependsOn:
  - name: rabbitmq-base
    namespace: rabbitmq